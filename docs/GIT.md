# EP-003 Git Instructions

After extracting this package into the repository root:

```bash
git add .
git commit -m "EP-003: Add project documentation memory system"
git push
```

## EP-032 Commit
After applying `CiteJury_EP-032.zip` over the existing repository, run:

```bash
git add .
git commit -m "EP-032: Add citation authority foundation"
git push
```

No file deletion is required for EP-032.

